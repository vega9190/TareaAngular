import { NombreUpperCasePipe } from './nombre-upper-case.pipe';

describe('NombreUpperCasePipe', () => {
  it('create an instance', () => {
    const pipe = new NombreUpperCasePipe();
    expect(pipe).toBeTruthy();
  });
});
